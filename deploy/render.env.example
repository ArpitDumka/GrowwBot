# Set these in Render → mf-faq-api → Environment (do not commit secrets).
GROQ_API_KEY=
PYTHONUNBUFFERED=1
# After Vercel deploy, optional custom origin:
# CORS_EXTRA_ORIGINS=https://your-project.vercel.app
