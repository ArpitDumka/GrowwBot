# Copy to .env locally (do not commit .env)
# Phase 7 — Groq LLM (see phase-1/config/llm.yaml)
GROQ_API_KEY=

# Production: set GROQ_API_KEY in Render dashboard only (see deploy/render.env.example)
# Frontend: set NEXT_PUBLIC_API_URL in Vercel (see deploy/vercel.env.example)
